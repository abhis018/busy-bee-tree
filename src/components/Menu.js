import {Link} from "react-router-dom";

const Menu = () => {

  return (
    <div className="gallery">
        <Link to="/">
            <button>Back to Home</button>
        </Link>
    </div>
  )
};

export default Menu;